import styled from 'styled-components';

export const Button = styled.div`
  svg {
    font-size: 45px;
    color: #52488c;
    transition: all 0.5s;
  }

  svg:hover {
    transform: scale(1.15);
  }
`;
